@model IEnumerable<AuthenticationTest.Models.tbl_roles>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.role_name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.role_name)
            </td>
            <td>
                @{
                    if (item.role_name == "Admin" || item.role_name == "Super Admin")
                    {
                        @Html.ActionLink("Details", "Details", new { id = item.Role_Id });
                    }
                    else
                    {
                        @Html.ActionLink("Edit", "Edit", new { id = item.Role_Id });
                            <text>|</text>
                        @Html.ActionLink("Details", "Details", new { id = item.Role_Id }); <text>|</text>
                        @Html.ActionLink("Delete", "Delete", new { id = item.Role_Id });
                    }
                } 

            </td>
        </tr>
                    }

</table>
